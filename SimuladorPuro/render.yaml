services:
  - type: web
    name: packing-simulator
    buildCommand: "npm install && npm run build"
    startCommand: "npm start"
    env: node
    envVars: 
      - key: NODE_ENV
        value: production 
